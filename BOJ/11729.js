// const fs = require("fs");
// const input = fs.readFileSync("/dev/stdin").toString().split(" ");

// var N = input[0];
var count = 0;
var one = [];
var two = [];
var three = [];

for (var i = 0; i < N; i++) {
  one[i] = i;
}

function hanoi(n) {
  if (three.length === N) break;
  while ()
}

hanoi(3);
