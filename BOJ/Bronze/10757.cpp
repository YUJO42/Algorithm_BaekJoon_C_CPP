#include <stdio.h>

int ft_strlen(char *str)
{
	int count = 0;

	while(str[count])
		count++;
	return (count);	
}

int main(void)
{
	char a[10001];
	char b[10001];
	char sum[10002];
	
	int a_len = ft_strlen(a);
	int b_len = ft_strlen(b);
	if (ft_strlen(a) != ft_str)
	
	return (0);
}
